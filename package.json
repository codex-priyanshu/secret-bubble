{
  "name": "secret-bubble",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "cd backend && npm install && node server.js"
  }
}